# Port the API listens on. Railway sets PORT automatically.
PORT=3001

# Allowed CORS origin in production (your web service URL, no trailing slash).
# Leave unset to allow all origins (fine for local dev).
WEB_ORIGIN=
